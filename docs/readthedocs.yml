version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

conda:
  environment: readthedocs-conda-environment.yml

build:
  jobs:
    post_create_environment:
      - echo "export LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH" >> $BASH_ENV
